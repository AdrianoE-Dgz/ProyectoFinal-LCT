import './Homepage.css'

function Homepage() {
  // const [count, setCount] = useState(0)

  return (
    <section id="generalContainer">
      <p>Homepage works!</p>
    </section>
  )
}

export default Homepage